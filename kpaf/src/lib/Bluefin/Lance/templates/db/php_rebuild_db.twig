#Data files list
#This file is automatically generated and should not be edited!

#Files below will be executed one by one to rebuild the target database.

#Begin of list

#Drop tables
../schema/drop_{{ schemaName }}.sql

#Create tables
{% for entity in entities %}
../schema/{{ schemaName }}/{{ entity }}.sql
{% endfor %}

#Add relationships

{% for relation in relations %}
../schema/{{ schemaName }}/{{ relation }}_relations.sql
{% endfor %}

#End of list