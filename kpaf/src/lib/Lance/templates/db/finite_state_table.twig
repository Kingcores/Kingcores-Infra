<?php
//Don't edit this file which is generated by Bluefin Lance.
namespace {{ namespace }}\Model\{{ schemaName }};

use Bluefin\Convention;

class {{ className }}
{
{% for state, comment in entity.getData %}
    const {{ state|const }} = '{{ state }}';
{% endfor %}

    private static $_data;

    public static function getDictionary()
    {
        if (!isset(self::$_data))
        {
            self::$_data = array(
{% for state, comment in entity.getData %}
                self::{{ state|const }} => _T({{ comment|format_value|raw }}, Convention::LOCALE_METADATA_DOMAIN),
{% endfor %}
            );
        }

        return self::$_data;
    }

    public static function getStates()
    {
        $data = self::getDictionary();
        return array_keys($data);
    }

    public static function getStateComment($state)
    {
        $data = self::getDictionary();
        return $data[$state];
    }
}